\section{The Double Centralizer Theorem}





% \subsubsection{Revisiting Isotypical Components}
% 
% 
% \begin{conventions}
%   In the following $R$ denotes a ring, $M$ denotes an $R$-module and $E$ denotes a simple $R$-module.
% \end{conventions}
% 
% 
% \begin{corollary}
%   If $M$ is semisimple then $M = \bigoplus_{[E] \in \Irr(R)} M_E$ is a decomposition into simple $(R' \otimes_\Integer R)$-submodules, and it is the unique such decomposition.
% \end{corollary}
% 
% 
% \begin{lemma}
%   Let $M$ be semisimple.
%   \begin{enumerate}
%     \item
%       The $R'(M)$-module $\Hom_R(E,M)$ is simple.
%     \item
%       We have that $\End_{R'(M)}( \Hom_R(E,M) ) \cong \End_R(E)^\op$, with the action of $\End_{R'(M)}( \Hom_R(E,M) )$ on $\Hom_R(E,M)$ corresponding to the action of $\End_R(E)^\op$ on $\Hom_R(E,M)$.
%   \end{enumerate}
% 
% \end{lemma}
